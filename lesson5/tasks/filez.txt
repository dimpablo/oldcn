=== 📄 task1.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Вопросы о дожде</title>
</head>
<body>
<h2>Какой оттенок значения приобретает частица 其 в вопросительных предложениях?</h2>
<p><strong>Пример: «丙寅卜爭 貞 其雨» ... «癸亥 貞 不其雨»</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">bǐng yín bǔ zhēng zhēn qí yǔ ... guǐ hài zhēn bù qí yǔ</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Указание на прошедшее время: «Дождь уже прошел»", correct: false },
    { text: "Частица возможности: «Будет ли дождь?»", correct: true },
    { text: "Повелительное значение: «Пусть пойдет дождь!»", correct: false },
    { text: "Утверждение: «Дождь действительно прошел»", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 其 в вопросах выражает возможность: «Будет ли...?»';
    } else {
      resultEl.innerHTML = '❌ Неверно. Подумайте, как 其 сочетается с 貞 и отрицанием.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task2.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Инверсия с 不我</title>
</head>
<body>
<h2>Как правильно перевести предложение 帝不我降熯?</h2>
<p><strong>Подсказка: обратите внимание на порядок слов и отрицание</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">dì bù wǒ jiàng hàn</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Я не получаю благословение от Небес", correct: false },
    { text: "Небеса не посылают мне зной", correct: true },
    { text: "Я не посылаю зной Небесам", correct: false },
    { text: "Небеса не принимают мой дар", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 不我降熯 — инверсия: "не посылают мне зной".';
    } else {
      resultEl.innerHTML = '❌ Неверно. Вспомните: при инверсии дополнение ставится после отрицания.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task3.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Двойное дополнение</title>
</head>
<body>
<h2>Какой глагол требует двойного дополнения в предложении 帝受我年?</h2>
<p><strong>Пример: «帝受我年» — Небеса посылают нам урожай</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">dì shòu wǒ nián</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "帝", correct: false },
    { text: "受", correct: true },
    { text: "我", correct: false },
    { text: "年", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 受 требует косвенного (мне) и прямого (урожай) дополнения.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Какой глагол выражает действие "послать"?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task4.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Порядок слов</title>
</head>
<body>
<h2>Какой порядок слов используется в конструкции 帝不我受佑?</h2>
<p><strong>Перевод: Небеса не посылают нам помощь</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">dì bù wǒ shòu yòu</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Прямой порядок: Подлежащее — Сказуемое — Дополнение", correct: false },
    { text: "Инверсия дополнения после отрицания", correct: true },
    { text: "Пассивный залог", correct: false },
    { text: "Повелительное наклонение", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 不我受佑 — классическая инверсия дополнения.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Обратите внимание на положение 我 после 不.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task5.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Отрицание и местоимение</title>
</head>
<body>
<h2>Почему в предложении 帝不我受佑 используется 不, а не 弗?</h2>
<p><strong>Сравните: 帝弗受佑 vs 帝不我受佑</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">dì fú shòu yòu vs dì bù wǒ shòu yòu</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Потому что 弗 не употребляется с личными местоимениями", correct: true },
    { text: "Потому что 不 сильнее по значению", correct: false },
    { text: "Это ошибка переписчика", correct: false },
    { text: "Потому что 受 — безличный глагол", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 弗 не используется при инверсии с местоимением.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Вспомните ограничения употребления 弗.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task6.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Перевод инверсии</title>
</head>
<body>
<h2>Как правильно перевести 帝不我其受佑?</h2>
<p><strong>Обратите внимание на частицу 其</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">dì bù wǒ qí shòu yòu</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Небеса не посылают мне помощь", correct: true },
    { text: "Небеса посылают мне помощь", correct: false },
    { text: "Я не посылаю помощь Небесам", correct: false },
    { text: "Небеса не принимают мою молитву", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 其 здесь — часть вопроса, не влияет на отрицание.';
    } else {
      resultEl.innerHTML = '❌ Неверно. 其 не отменяет отрицание 不.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task7.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Грамматическая структура</title>
</head>
<body>
<h2>Какая грамматическая структура используется в предложении 洹弗作兹邑祸?</h2>
<p><strong>Перевод: Река Хуан не причинит беды этому городу</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">huán fú zuò zī yì huò</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Инверсия дополнения с 不", correct: false },
    { text: "Отрицание с 弗 и прямое дополнение", correct: true },
    { text: "Пассивный залог", correct: false },
    { text: "Повелительное наклонение", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> Здесь нет инверсии, используется 弗.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Обратите внимание: нет местоимения после отрицания.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task8.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Личные местоимения</title>
</head>
<body>
<h2>Какое местоимение означает "мы" в древнекитайском языке?</h2>
<p><strong>Выберите правильный вариант</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "我", correct: true },
    { text: "余", correct: false },
    { text: "汝", correct: false },
    { text: "其", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 我 — "мы", 余 — "я".';
    } else {
      resultEl.innerHTML = '❌ Неверно. Вспомните, какое местоимение используется в текстах с коллективным подлежащим.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task9.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Сравнение местоимений</title>
</head>
<body>
<h2>Чем отличаются 我 и 余?</h2>
<p><strong>Сравните: 余受年 и 我受年</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">yú shòu nián vs wǒ shòu nián</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "我 — только «я», 余 — только «мы»", correct: false },
    { text: "余 — «я», 我 — «мы» или «я»", correct: true },
    { text: "Они взаимозаменяемы", correct: false },
    { text: "余 — устаревшее, 我 — современное", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 余 — только 1-е л. ед.ч., 我 — и «я», и «мы».';
    } else {
      resultEl.innerHTML = '❌ Неверно. Обратитесь к грамматическому комментарию.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task10.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Перевод с русского</title>
</head>
<body>
<h2>Как будет на древнекитайском: "Небеса не посылают мне урожай"?</h2>
<p><strong>Используйте инверсию</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "帝不我受年", correct: true },
    { text: "帝弗受我年", correct: false },
    { text: "我弗受年", correct: false },
    { text: "帝不年受我", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> Правильная инверсия: 不我受年.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Вспомните: после 不 — местоимение, затем глагол.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task11.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Глагол 作</title>
</head>
<body>
<h2>Что означает глагол 作 в контексте 洹作兹邑祸?</h2>
<p><strong>Перевод: река Хуан причиняет беду городу</strong></p>
<div class="pinyin-container">
  <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
  <div class="pinyin-text">huán zuò zī yì huò</div>
</div>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Создавать, причинять", correct: true },
    { text: "Останавливать", correct: false },
    { text: "Защищать", correct: false },
    { text: "Просить", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 作 — "причинять", "созидать", в зависимости от контекста.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Подумайте, что делает река с городом.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task12.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Конструкция 受我年</title>
</head>
<body>
<h2>Какой тип дополнения выражает 我 в предложении 帝受我年?</h2>
<p><strong>Варианты: прямое, косвенное, обстоятельство</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Прямое дополнение", correct: false },
    { text: "Косвенное дополнение", correct: true },
    { text: "Обстоятельство времени", correct: false },
    { text: "Подлежащее", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 我 — косвенное дополнение (кому?).';
    } else {
      resultEl.innerHTML = '❌ Неверно. Кому посылают урожай?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task13.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Грамматический анализ</title>
</head>
<body>
<h2>Выделите сказуемое в предложении 帝不我降熯</h2>
<p><strong>Выберите один элемент</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "帝", correct: false },
    { text: "不", correct: false },
    { text: "我", correct: false },
    { text: "降熯", correct: true }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> Сказуемое — 降熯.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Что делают Небеса?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task14.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Словарь</title>
</head>
<body>
<h2>Что означает 熯 в контексте 帝不我降熯?</h2>
<p><strong>Подсказка: связано с погодой</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Дождь", correct: false },
    { text: "Зной, жара", correct: true },
    { text: "Урожай", correct: false },
    { text: "Молитва", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 熯 — зной, жара.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Вспомните, что может посылать или не посылать Небо.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task15.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Перепись текста</title>
</head>
<body>
<h2>Перепишите предложение 帝受我年 в прямом порядке</h2>
<p><strong>Без инверсии</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "帝受年我", correct: true },
    { text: "我年受帝", correct: false },
    { text: "年我受帝", correct: false },
    { text: "帝年受我", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> Прямой порядок: 帝受年我.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Кому посылают? Что посылают?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task16.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Отрицание 弗</title>
</head>
<body>
<h2>Когда используется 弗 вместо 不?</h2>
<p><strong>Выберите правильное условие</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Перед местоимением в инверсии", correct: false },
    { text: "Перед глаголом без дополнения", correct: true },
    { text: "В вопросах", correct: false },
    { text: "В повелительных предложениях", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 弗 — для прямого отрицания глаголов без местоимения.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Вспомните ограничения употребления 弗.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task17.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Перевод</title>
</head>
<body>
<h2>Переведите: "Река Хуан не причинит беды этому городу"</h2>
<p><strong>Выберите правильный вариант</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "洹弗作兹邑祸", correct: true },
    { text: "洹不作兹邑祸", correct: false },
    { text: "兹邑弗作洹祸", correct: false },
    { text: "祸作洹弗兹邑", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> Это дословный перевод из текста Урока 5.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Сравните с оригиналом.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task18.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Частица 其</title>
</head>
<body>
<h2>Какова функция 其 в предложении 帝不我其受佑?</h2>
<p><strong>Обратите внимание: предложение — вопрос</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Отрицание", correct: false },
    { text: "Частица возможного действия", correct: true },
    { text: "Прямое дополнение", correct: false },
    { text: "Подлежащее", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 其 — частица возможного действия в вопросах.';
    } else {
      resultEl.innerHTML = '❌ Неверно. 其 не отрицает, а указывает на возможность.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task19.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Подлежащее</title>
</head>
<body>
<h2>Кто является подлежащим в предложении 帝不我降熯?</h2>
<p><strong>Выберите один вариант</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "我", correct: false },
    { text: "帝", correct: true },
    { text: "熯", correct: false },
    { text: "不", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> Подлежащее — 帝.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Кто не посылает зной?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task20.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Лексика</title>
</head>
<body>
<h2>Что означает 兹 в выражении 兹邑?</h2>
<p><strong>Подсказка: указательное местоимение</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Тот", correct: false },
    { text: "Этот", correct: true },
    { text: "Каждый", correct: false },
    { text: "Некоторый", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 兹 — "этот".';
    } else {
      resultEl.innerHTML = '❌ Неверно. Вспомните, как переводится 兹邑.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task21.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Синтаксис</title>
</head>
<body>
<h2>Где находится косвенное дополнение в предложении 帝受我年?</h2>
<p><strong>Выберите позицию</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Перед сказуемым", correct: false },
    { text: "После сказуемого", correct: true },
    { text: "В начале предложения", correct: false },
    { text: "Не выражено", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 我 стоит после 受 — косвенное дополнение.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Где стоит 我?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task22.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Инверсия</title>
</head>
<body>
<h2>Какое предложение содержит инверсию дополнения?</h2>
<p><strong>Выберите один вариант</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "帝受我年", correct: false },
    { text: "帝不我受年", correct: true },
    { text: "我受年", correct: false },
    { text: "年受我", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 不我受年 — инверсия.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Где дополнение перед глаголом?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task23.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Перевод</title>
</head>
<body>
<h2>Переведите: "Небеса посылают нам урожай"</h2>
<p><strong>Выберите правильный вариант</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "帝受我年", correct: true },
    { text: "我受帝年", correct: false },
    { text: "年受我帝", correct: false },
    { text: "帝年受我", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> Это стандартная конструкция Урока 5.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Кто кому что посылает?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task24.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Грамматика</title>
</head>
<body>
<h2>Какой глагол НЕ требует двойного дополнения?</h2>
<p><strong>Выберите один</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "受", correct: false },
    { text: "作", correct: false },
    { text: "降", correct: false },
    { text: "有", correct: true }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> 有 — "иметь", не требует косвенного дополнения.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Какой глагол выражает "иметь"?';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task25.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Задание: Обобщение</title>
</head>
<body>
<h2>Что общего у предложений 帝不我降熯 и 帝不我受佑?</h2>
<p><strong>Выберите правильное утверждение</strong></p>
<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>
<script>
  const options = [
    { text: "Оба содержат инверсию дополнения с 不", correct: true },
    { text: "Оба используют 弗", correct: false },
    { text: "Оба — повелительные", correct: false },
    { text: "Оба — пассивные", correct: false }
  ];
  
  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }
  
  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) return;
    
    container.innerHTML = "";
    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = index;
      input.dataset.correct = opt.correct;
      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }
  
  function getSelectedValue(elementId) {
    return document.querySelector(`#${elementId} input:checked`)?.dataset.correct || '';
  }
  
  function check(answersId, resultId, options) {
    const resultEl = document.getElementById(resultId);
    const selected = getSelectedValue(answersId);
    
    if (!selected) {
      resultEl.innerHTML = '❌ Выберите один из вариантов.';
      return;
    }
    
    if (selected === 'true') {
      resultEl.innerHTML = '✅ <b>Верно!</b> Оба — примеры инверсии с 不.';
    } else {
      resultEl.innerHTML = '❌ Неверно. Сравните структуру предложений.';
    }
  }
  
  window.onload = renderOptions;
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>