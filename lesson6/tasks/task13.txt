<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Урок 6: Инверсия или нет?</title>
</head>
<body>
<h2>Определите тип конструкции</h2>
<div class="instructions-header" onclick="toggleInstructions()">📌 Инструкция (нажми для развертывания) ▼</div>
<div class="instructions-content" style="display:none">
    <p><strong>Инверсия дополнения</strong> — обязательна при:</p>
    <ul>
        <li>Отрицании <strong>不</strong></li>
        <li>Местоимённом дополнении: <strong>我, 余, 朕</strong></li>
    </ul>
    <p>Пример: <strong>不我降熯</strong> — ✅ инверсия</p>
    <p><strong>Стандартный порядок</strong> — во всех остальных случаях.</p>
    <p>Пример: <strong>帝受我佑</strong> — ❌ не инверсия</p>
</div>
<div class="columns">
    <div id="column1" class="column"></div>
    <div id="column2" class="column"></div>
</div>
<button id="check-btn">Проверить</button>
<button id="reset-btn">Сбросить</button>
<p id="result"></p>
<script>
    const sentences = [
        { text: "不我降熯", type: "inversion", explanation: "не пошлёт ли нам засуху? — инверсия при 不 + 我" },
        { text: "帝受我佑", type: "standard", explanation: "дарует нам помощь — стандартный порядок" },
        { text: "帝不我受佑", type: "inversion", explanation: "не дарует ли нам помощь? — инверсия" },
        { text: "不余降災", type: "inversion", explanation: "не пошлёт ли он бедствие нам?" },
        { text: "土方征于我東鄙", type: "standard", explanation: "племя напало на нашу окраину — стандарт" },
        { text: "不我熯", type: "inversion", explanation: "не иссушит ли нас?" },
        { text: "允有來艱", type: "standard", explanation: "действительно пришло бедствие" },
        { text: "王令婦好伐", type: "standard", explanation: "приказывает нападать — стандарт" }
    ];

    function shuffle(arr) {
        return [...arr].sort(() => Math.random() - 0.5);
    }

    function createItem(s, index) {
        const el = document.createElement('div');
        el.className = 'sentence-item';
        el.dataset.type = s.type;
        el.innerHTML = `
            <div class="oracle-text">${s.text}</div>
            <div class="options">
                <label><input type="radio" name="q${index}" value="inversion"> инверсия</label>
                <label><input type="radio" name="q${index}" value="standard"> стандарт</label>
            </div>
        `;
        return el;
    }

    function init() {
        const shuffled = shuffle(sentences);
        const col1 = document.getElementById('column1');
        const col2 = document.getElementById('column2');
        col1.innerHTML = '';
        col2.innerHTML = '';
        shuffled.slice(0, 4).forEach((s, i) => col1.appendChild(createItem(s, i)));
        shuffled.slice(4).forEach((s, i) => col2.appendChild(createItem(s, i + 4)));
    }

    document.getElementById("check-btn").addEventListener("click", () => {
        const items = document.querySelectorAll('.sentence-item');
        let correct = 0;
        items.forEach((item, i) => {
            const selected = item.querySelector('input:checked');
            const expected = item.dataset.type;
            if (selected && selected.value === expected) correct++;
            item.style.border = (selected && selected.value === expected) ? '2px solid #4CAF50' : '2px solid #f44336';
        });
        const result = document.getElementById("result");
        if (correct === 8) {
            result.innerHTML = "✅ Все верно! Вы усвоили правило инверсии.";
            sendResult(true);
        } else {
            result.innerHTML = `❌ ${correct}/8 верно. Обратите внимание на конструкции с 不 + 我/余.`;
            sendResult(false);
        }
        result.style.display = 'block';
    });

    document.getElementById("reset-btn").addEventListener("click", () => {
        init();
        document.getElementById("result").style.display = 'none';
    });

    function sendResult(success) {
        window.parent.postMessage({ type: "TASK_RESULT", success }, "*");
    }

    function toggleInstructions() {
        const content = document.querySelector('.instructions-content');
        const header = document.querySelector('.instructions-header');
        const isHidden = content.style.display === 'none';
        content.style.display = isHidden ? 'block' : 'none';
        header.innerHTML = `📌 Инструкция (нажми для развертывания) ${isHidden ? '▲' : '▼'}`;
    }

    init();
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>